/* exported last */

/* Pseudocode

Goal: return last element of array
-Declare a function with its name and parameter
-Access the last index of the array to get the last element

*/

function last(array) {
  return array[array.length - 1];
}
