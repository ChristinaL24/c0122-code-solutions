/* exported getValue */

/* Pseudocode

Goal: return the value in object at the key property
-Declare a function with its name and parameter
-Return object at key from the function

*/

function getValue(object, key) {
  return object[key];
}
