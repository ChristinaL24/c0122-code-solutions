import React from 'react';
import ReactDOM from 'react-dom';
import HotButton from './hot-button';

ReactDOM.render(
  <HotButton label="Hot Button" />,
  document.querySelector('#root')
);

// 1. do not build component, make sure react is working first & output something

// this page is the entry point, it starts everything
